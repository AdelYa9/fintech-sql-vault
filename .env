# This defines the master username (superuser) for the PostgreSQL instance
POSTGRES_USER=vault_master

# This establishes the master password for that superuser account
POSTGRES_PASSWORD=super_secure_password_123

# This tells the container to automatically create a blank database with this specific name when it first boots up
POSTGRES_DB=fintech_vault